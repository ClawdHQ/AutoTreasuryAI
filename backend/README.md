# Backend

API and services for AutoTreasury AI.

## Structure

- `api/` - REST/GraphQL API endpoints
- `services/` - Business logic and blockchain interaction services
- `db/` - Database schemas and migrations

## Setup

```bash
pnpm install
pnpm dev
```
